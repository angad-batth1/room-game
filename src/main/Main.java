package main;

import view.GameWindow;

/**
 * This is the main driver class that launches the room game application.
 * @author Gurangad Batth
 */
public class Main{
    /**
     * This is the main method that starts the game window.
     * @param args command line arguments passed into the program
     */
    public static void main(String[] args) {
        // Initialize the game window which starts the application
        new GameWindow();
    }

}
